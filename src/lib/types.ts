export type FormState = {
  firstName: string;
  lastName: string;
  email: string;
  message: string;
};

export type Testimonial = {
  name: string;
  role: string;
  company: string;
  content: string;
  image?: string;
};